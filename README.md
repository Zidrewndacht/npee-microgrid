To run:

```
docker compose up --build
```


Done:

Docker-compose:
    collector
    simulator
    flask
        gunicorn

Still missing:
    nginx
        http3
        gzip
    
load/save dashboard
